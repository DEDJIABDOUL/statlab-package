% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tests.R
\name{st_compare}
\alias{st_compare}
\title{Comparer une variable selon un groupe}
\usage{
st_compare(data, variable, group, config, paired = FALSE)
}
\arguments{
\item{data}{Un data.frame (typiquement le resultat de \code{\link[=st_prepare]{st_prepare()}}).}

\item{variable}{Nom (chr) de la variable comparee.}

\item{group}{Nom (chr) de la variable de groupement (ou de la seconde
variable continue, pour une correlation).}

\item{config}{Un objet \code{statlab_config} valide, tel que retourne par
\code{\link[=st_validate_config]{st_validate_config()}}.}

\item{paired}{Si \code{TRUE}, traite la comparaison comme appariee (requiert
une variable de nature \code{identifiant} dans le dictionnaire).}
}
\value{
Une liste (classe \code{statlab_comparison}) avec les champs
\code{variable}, \code{group}, \code{test_name}, \code{statistic}, \code{parameter},
\code{p_value}, \code{effect_size}, \code{effect_size_name}, \code{conf_low},
\code{conf_high}, \code{n_by_group}, \code{descriptive_by_group}, \code{justification},
\code{rules_triggered}, \code{derogations}.
}
\description{
Construit le contexte methodologique (natures, effectifs, normalite,
homogeneite des variances, effectifs theoriques, appariement), le
soumet au moteur de regles (\code{\link[=st_evaluate_rules]{st_evaluate_rules()}}), puis execute le
test statistique retenu par l'arbre de decision (ou impose par
\verb{config$analyse$comparaisons[].forcer_test}).
}
\details{
Arbre de decision : \code{group} continu et \code{variable} continue ->
correlation (Pearson, sauf regle de normalite -> Spearman). Sinon, si
\code{paired = TRUE} -> test t/Wilcoxon apparie (variable continue) ou test
de McNemar (variable binaire), l'appariement des observations reposant
sur la variable de nature \code{identifiant} du dictionnaire. Sinon (non
apparie) : variable continue et 2 groupes -> Student/Welch/Mann-Whitney ;
variable continue et 3+ groupes -> ANOVA/ANOVA de Welch/Kruskal-Wallis ;
variable categorielle -> Chi2/Fisher exact.
}
