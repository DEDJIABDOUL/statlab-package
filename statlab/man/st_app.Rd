% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{st_app}
\alias{st_app}
\title{Lancer l'interface graphique locale}
\usage{
st_app(project_dir = getwd())
}
\arguments{
\item{project_dir}{Repertoire (chr) du projet a ouvrir au demarrage.
Par defaut, le repertoire courant. Cree s'il n'existe pas encore un
\code{config.yml} (le projet peut demarrer vide, sources incluses).}
}
\value{
Invisiblement, \code{NULL}. La fonction bloque jusqu'a la fermeture
de l'application (comportement standard de \code{\link[shiny:runApp]{shiny::runApp()}}).
}
\description{
Lance une application Shiny locale permettant de piloter un projet
statlab de bout en bout : declaration des sources, audit, construction
de \code{config.yml} (dictionnaire, reconciliation, plan d'analyse),
consultation des resultats, generation du rapport.
}
\details{
L'interface est une couche mince au-dessus du package : elle lit et
ecrit \code{config.yml}, et appelle les memes fonctions que la ligne de
commande. Tout projet traite via l'interface produit un \code{config.yml}
qui, rejoue en ligne de commande (\verb{statlab rapporter --config=...}, ou
\code{\link[=st_report]{st_report()}}), produit un resultat identique.

L'application n'ecoute que sur l'interface reseau locale (\verb{127.0.0.1})
et ne met en oeuvre aucune authentification : elle n'est pas concue pour
etre exposee au-dela de la machine de l'operateur.
}
