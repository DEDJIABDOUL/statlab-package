% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rules.R
\name{st_evaluate_rules}
\alias{st_evaluate_rules}
\title{Evaluer le referentiel methodologique sur un contexte}
\usage{
st_evaluate_rules(context, family = NULL, override = character(0))
}
\arguments{
\item{context}{Liste nommee de valeurs (numeriques, logiques ou
caracteres) decrivant la situation a evaluer (ex : \code{test_type},
\code{is_continuous}, \code{min_group_n}, \code{shapiro_p}, ...).}

\item{family}{Famille de regles (chr) a evaluer. Si \code{NULL}, toutes les
regles du referentiel sont considerees.}

\item{override}{Vecteur (chr) d'identifiants de regles a contourner
(derogation operateur, ex : \verb{analyse.comparaisons[].forcer_test}).}
}
\value{
Un data.frame des regles declenchees, trie par severite
(bloquant, puis avertissement, puis information), avec les colonnes
\code{id}, \code{famille}, \code{severite}, \code{message}, \code{action}, \code{justification},
\code{source}, \code{derogation}.
}
\description{
Evalue chaque regle applicable (eventuellement filtree par \code{family})
sur \code{context}. Comportements obligatoires :
\itemize{
\item une regle bloquante SANS action qui se declenche arrete l'execution ;
\item une regle bloquante AVEC action qui se declenche applique l'action et
consigne la justification (journal, niveau "info"), en interpolant
les valeurs du contexte ;
\item un identifiant de regle present dans \code{override} est traite comme une
derogation : la regle n'arrete pas l'execution et n'applique pas son
action, mais la derogation est journalisee (niveau "derogation") avec
l'identifiant de la regle contournee ;
\item les regles non bloquantes (avertissement, information) sont
journalisees mais n'arretent jamais l'execution.
}
}
