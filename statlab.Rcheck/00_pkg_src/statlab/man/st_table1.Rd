% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table1.R
\name{st_table1}
\alias{st_table1}
\title{Construire le Tableau 1}
\usage{
st_table1(data, config)
}
\arguments{
\item{data}{Un data.frame (typiquement le resultat de \code{\link[=st_prepare]{st_prepare()}}).}

\item{config}{Un objet \code{statlab_config} valide, tel que retourne par
\code{\link[=st_validate_config]{st_validate_config()}}, dont \code{analyse$tableau_1} declare les
variables (et, en option, la stratification et le denominateur des
pourcentages).}
}
\value{
Un objet \code{gtsummary} (\code{tbl_summary}), utilisable pour
composition ulterieure (ex : \code{\link[gtsummary:tbl_merge]{gtsummary::tbl_merge()}}) ou converti
via \code{\link[=st_table1_flextable]{st_table1_flextable()}} / \code{\link[=st_table1_csv]{st_table1_csv()}}.
}
\description{
Construit le tableau descriptif declare dans \code{config$analyse$tableau_1},
en utilisant les libelles, unites et ordres de modalites du
dictionnaire (jamais les noms techniques). Pour chaque variable
continue, la statistique (moyenne/ecart-type ou mediane/[Q1-Q3]) est
choisie selon la normalite (Shapiro-Wilk), evaluee par le meme moteur
de regles que \code{\link[=st_compare]{st_compare()}}. Si une stratification est declaree, une
colonne de p-value est ajoutee ; le test employe pour chaque variable
est lui aussi choisi par \code{\link[=st_compare]{st_compare()}} (jamais un choix par defaut de
gtsummary), et consigne dans une note de bas de tableau.
}
