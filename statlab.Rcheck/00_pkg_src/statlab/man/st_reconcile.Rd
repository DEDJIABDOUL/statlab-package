% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile.R
\name{st_reconcile}
\alias{st_reconcile}
\title{Assembler plusieurs sources selon la configuration}
\usage{
st_reconcile(sources, config)
}
\arguments{
\item{sources}{Liste nommee de data.frame (typiquement le resultat de
\code{\link[=st_read_all_sources]{st_read_all_sources()}}).}

\item{config}{Un objet \code{statlab_config} valide, tel que retourne par
\code{\link[=st_validate_config]{st_validate_config()}}, dont la section \code{reconciliation} declare les
operations a executer.}
}
\value{
Une liste avec \code{table} (le data.frame produit par la derniere
operation) et \code{audit} (une liste d'audits, un par operation, nommee
par le \code{resultat} de chaque operation).
}
\description{
Execute, dans l'ordre declare, les operations de la section
\code{reconciliation} de \code{config.yml} (empiler, joindre, pivoter_long). Le
\code{resultat} de chaque operation devient disponible comme source pour les
operations suivantes, au meme titre que les identifiants de \code{sources}.
}
