% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_script.R
\name{st_export_script}
\alias{st_export_script}
\title{Generer le script R autonome de reproduction de l'analyse}
\usage{
st_export_script(config, output = "sorties/rapport/analyse.R")
}
\arguments{
\item{config}{Un objet \code{statlab_config} valide, tel que retourne par
\code{\link[=st_validate_config]{st_validate_config()}}.}

\item{output}{Chemin (chr) du script a produire. Si relatif, resolu par
rapport au repertoire du projet. Par defaut,
\code{"sorties/rapport/analyse.R"}.}
}
\value{
Le chemin absolu (chr) du script genere, de maniere invisible.
}
\description{
Execute la chaine d'analyse declaree dans \code{config} (lecture,
reconciliation, preparation, comparaisons) pour en capturer les faits
resolus (parametres de lecture detectes, tests statistiques retenus par
le moteur de regles), puis genere un script R autonome qui reproduit
litteralement chaque etape avec des packages publics du CRAN
uniquement : aucun appel a \code{statlab} n'apparait dans le fichier produit.
}
\details{
Chaque test statistique est accompagne, en commentaire, de la
justification methodologique produite par \code{\link[=st_evaluate_rules]{st_evaluate_rules()}} (via
\code{\link[=st_compare]{st_compare()}}). Les graphiques utilisent un theme neutre (\code{ggplot2}) :
la charte graphique du rapport (\code{R/theme.R}) n'est pas reproduite, afin
de ne pas introduire de dependance au package \code{statlab}.
}
