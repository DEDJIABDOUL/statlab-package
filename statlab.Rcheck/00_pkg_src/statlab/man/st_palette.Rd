% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{st_palette}
\alias{st_palette}
\title{Palette de couleurs de la charte graphique}
\usage{
st_palette(
  n,
  type = c("qualitative", "sequentielle", "divergente"),
  variant = c("ecran", "impression", "projection")
)
}
\arguments{
\item{n}{Nombre (entier positif) de couleurs demandees.}

\item{type}{Nature de la palette : categorielle (\code{"qualitative"}),
magnitude continue a une teinte (\code{"sequentielle"}), ou deux poles
opposes (\code{"divergente"}).}

\item{variant}{Declinaison graphique (cf. \code{\link[=st_theme]{st_theme()}}).}
}
\value{
Un vecteur (chr) de \code{n} couleurs hexadecimales.
}
\description{
Palettes definies dans \code{inst/templates/palettes.yml} (editable sans
toucher au code). Pour \code{variant = "impression"}, retourne toujours une
palette en niveaux de gris (jamais de couleur imprimee), quel que soit
\code{type} : le motif de remplissage et le type de trait (cf.
\code{inst/templates/palettes.yml}, champs \code{formes}/\code{types_trait}) portent
alors la distinction supplementaire.
}
\details{
Pour \code{type = "qualitative"}, la distinguabilite de la palette retenue
est verifiee en deuteranopie et en protanopie
(\code{\link[colorspace:deutan]{colorspace::deutan()}}/\code{\link[colorspace:protan]{colorspace::protan()}}) ainsi qu'apres
conversion en niveaux de gris (\code{\link[colorspace:desaturate]{colorspace::desaturate()}}) ; un
avertissement est emis (jamais silencieux) si \code{n} couleurs simultanees
reduisent cette distinguabilite sous le seuil declare dans
\code{palettes.yml}.
}
